# ---------------------------------------------------------------
# Copy this file to .env and fill in your values.
# Used for Docker and local/venv deployments only.
# Render deployments are configured via render.yaml - no .env needed.
# ---------------------------------------------------------------

# A long random string used to sign session cookies.
# Generate one with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-to-a-long-random-string

# ---------------------------------------------------------------
# PostgreSQL connection - change DB_PASS value before deploying.
#
# Generate a random password with:
#   python3 -c "import secrets; print(secrets.token_urlsafe(24))"
# ---------------------------------------------------------------
DB_HOST=localhost
DB_PORT=5432
DB_USER=matrimony_user
DB_PASS=change-me
DB_NAME=matrimony_db

# Optional: host port the app listens on (default 8000)
# APP_PORT=8000
